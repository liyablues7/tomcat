## 目录   

- 使用 JK 1.2.x 原生连接器   
- 使用 Apache HTTP Server 2.x 

## 使用 JK 1.2.x 原生连接器  

请参考 JK 1.2.x 文档。

## 使用 Apache HTTP Server 2.x 

请参阅 Apache HTTP Server 2.2 的 mod_proxy 文档。》。新版的 mod_proxy 不仅能够》》 Apache HTTP Server 2.0 ，而且必须使用 Apache HTTP Server 2.2 的代码独立编译。